
#imports
import os
from targets import buddhaland_targets
from tags import buddhaland_tags
from comments import buddhaland_comments

class InstagramProfiles:
    def __init__(username, password, targets, tags, comments)
        self.username = username
        self.password = password
        self.targets = targets
        self.tags = tags
        self.comments = comments
        
        
buddhaland_ = InstagramProfiles('buddhaland_', os.environ['buddhaland_pass'], buddhaland_targets, buddhaland_tags, buddhaland_comments)